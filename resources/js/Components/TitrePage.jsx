const TitrePage = ({ page }) => {
    return (
        <div className="w-full mt-2 items-center">
            <h2 className="font-semibold text-2xl dark:text-white text-gray-800 leading-tight">{page}</h2>
        </div>
    );
};

export default TitrePage;
